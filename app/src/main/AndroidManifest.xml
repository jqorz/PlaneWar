<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jqorz.planewar">

    <application
        android:name=".GameApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.notAnimation">
        <activity android:name=".frame.GameLauncher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".frame.GamePlaying" />
        <activity android:name=".frame.GameOver" />
        <activity android:name=".frame.GameAbout" />
        <activity
            android:name=".frame.GameSetting"
            android:theme="@style/dialog" />
        <activity
            android:name=".frame.GameCheat"
            android:theme="@style/dialog" />
    </application>

</manifest>